////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// NOTE imports
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

import { AxiosError } from 'axios';
import { ActionType } from 'typesafe-actions';
import * as errorsActions from './actions';

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// NOTE models
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

export enum ERRORS_ACTION_TYPES {
  LOGIN_ERROR = '@@errors/login_error',
  RESET_LOGIN_ERROR = '@@errors/reset_login_error',
}

export interface IStateErrors {
  loginError: AxiosError | null;
}

export type TErrorsActions = typeof errorsActions;
export type TErrorsActionType = ActionType<TErrorsActions>;
export type TActionOnLoginError = ActionType<typeof errorsActions.onLoginError>;
