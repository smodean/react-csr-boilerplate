import { RootState } from '@models/RootState';
import { initialStateLocalization } from '@store/data/localization/reducer';

export const rootStateMock: RootState = {
  localization: initialStateLocalization,
};
